//import express and controllers
import express from "express";
import cors from "cors";
import { MessagePort } from "worker_threads";




const port = 5000
applicationCache.addEventListener(port, ()=> console.log("App listening on port" + port))